Convex scaffold

- Initialize a Convex project here when enabling Convex:
  convex init
- Add schema and functions for real-time signals (e.g., content change notifications).
- Set env:
  CONVEX_DEPLOYMENT=
  CONVEX_SITE_URL=

This example does not enable Convex by default; see the main README for instructions.
