Netflix Conductor scaffold using official images via docker compose.

Run:
- docker compose up conductor-server conductor-ui
- UI: http://localhost:5000
- API: http://localhost:7101

Register a workflow via REST with a JSON definition.
